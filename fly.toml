app = "parking-bot"

# For a worker-style Telegram bot (polling), no HTTP server is exposed.
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[build]
  # Dockerfile will handle Python dependencies.

[env]
  PYTHONUNBUFFERED = "1"

